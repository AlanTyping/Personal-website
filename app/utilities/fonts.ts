import { Poppins } from 'next/font/google';

export const poppins500 = Poppins({
    weight: "500",
    subsets: ["latin"]
});

export const poppins800 = Poppins({
    weight: "800",
    subsets: ["latin"]
});
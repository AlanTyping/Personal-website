import { Poppins } from 'next/font/google';

export const poppins400 = Poppins({
    weight: "400",
    subsets: ["latin"]
});

export const poppins800 = Poppins({
    weight: "800",
    subsets: ["latin"]
});